# Contributing

1. Create a feature branch.
2. Install development dependencies with `make install`.
3. Run `make lint` and `make test` before opening a pull request.
4. Keep changes small and document important modeling decisions.
5. Use clear conventional commit messages such as `feat:`, `fix:`, `refactor:`, `test:`, `ci:`, and `docs:`.
